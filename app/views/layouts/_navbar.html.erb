<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Home', root_path, class:'navbar-brand ms-5' %>
    <% if user_signed_in? %>
        <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto mb-2 me-5">
            <li class="nav-item me-3">
                <%= link_to current_company.name, '#', class:'nav-link text-white text-bg-danger' %>
            </li>
            <li class="nav-item me-3">
                <%= link_to current_user.name, '#', class:'nav-link text-white' %>
            </li>
            <li class="nav-item">
                <%= button_to 'Sign Out', 
                    destroy_user_session_path,
                    method: :delete, 
                    class:'nav-link btn btn-outline-secondary text-white' %>
            </li>
        </ul>
        </div>
    <% else %>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto me-5">
                <li class="nav-item">
                    <%= link_to 'Sign In', 
                        new_user_session_path, 
                        class:'nav-link btn btn-outline-secondary text-white' %>
                </li>
            </ul>
        </div>
    <% end %>
  </div>
</nav>